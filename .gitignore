# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
# ----------------------------------------------------------------
# The node_modules directory contains all downloaded npm packages.
# It should never be committed as it can be regenerated from package-lock.json.
/node_modules

# Logs
# ----------------------------------------------------------------
# Log files generated by npm, yarn, or other tools.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Build artifacts and compiled files
# ----------------------------------------------------------------
# A general build directory for temporary build outputs.
# NOTE: The `/dist` directory IS COMMITTED to the repository for JavaScript actions.
# This is a best practice as it allows the action to run without a build step
# on the user's side, making it faster and more self-contained.
/build/

# Testing
# ----------------------------------------------------------------
# Coverage reports generated by testing frameworks like Jest.
/coverage
.nyc_output

# Environment variables
# ----------------------------------------------------------------
# Never commit environment variables or secrets.
# Use a .env.example file to provide a template for contributors.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Operating System generated files
# ----------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE and editor configuration
# ----------------------------------------------------------------
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains (WebStorm, IntelliJ, etc.)
.idea/

# Vim
*.swp

# Other
*.iml
*.bak

dist/

wrapper/dist/
